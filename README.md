# FF3_MultipleRegression_Test
This project analyzes the impact of Fama-French 3-factor model (FF3) on stock returns using data retrieved via yfinance. It employs a multiple regression approach from statsmodels to compute the statistical significance of each FF3 factor.
